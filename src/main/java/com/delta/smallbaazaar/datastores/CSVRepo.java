//package com.delta.smallbaazaar.datastores;
//
//import com.delta.smallbaazaar.entities.CSV;
//import org.springframework.data.jpa.repository.JpaRepository;
//import org.springframework.data.jpa.repository.Query;
//import org.springframework.stereotype.Repository;
//
//@Repository
//public interface CSVRepo extends JpaRepository<CSV, Integer> {
//
////    @Query("Select a from CSV a where user_id = ?1 and document_type = ?2")
//    public static CSV checkDocumentByUserId(Integer userId);
//
////    @Query("Select fileName from CSV a where user_id = ?1 and document_type = ?2")
////    String getUploadDocumnetPath(Integer userId, String docType);
//}
